import { GujaratiConversionPipe } from './gujarati-conversion.pipe';

describe('GujaratiConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new GujaratiConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
